Session Summary (2026-09-17)
- Fixed Vite build error caused by an unterminated comment in `src/components/DesktopIcon/DesktopIcon.jsx`.
- Implemented the complete structure for `DesktopIcon` component, matching the missing file body and properly using its props (label, x, y, type).
- Verified the build succeeds using `npm run build`.
- Updated `DesktopIcon.jsx` and `DesktopIcon.css` to dynamically render the current day and date (e.g. "Gio 17") in macOS calendar icon style when `type='calendar'`.
- Refactored the dock to use pure CSS native `:has()` selector for animations instead of JavaScript state updates, completely fixing jitter and overlap issues while greatly improving performance.
- Made desktop icons draggable by implementing mouse drag event handlers in `DesktopIcon.jsx`.
- Fixed the dock container height so that hovering icons overflow upwards outside the container, matching the native macOS dock behavior.
- Removed the secondary toolbar containing the zoom, markup, and search tools from `Window.jsx` as requested.
- Implemented a fully functional `NotesApp` component that 100% faithfully recreates the macOS Apple Notes UI and allows adding, editing, and deleting notes.
- Updated `DesktopPage` and `DesktopIcon` so the Notes app can be opened by double-clicking the Note icon on the desktop.
- Created `CalendarApp` and `StickyNote` components that open independently on double-clicking the Calendar icon, matching the requested reference image.
- Updated Chrome icon in the Dock to open a LinkedIn profile in a new tab when clicked.
- Fixed a global CSS issue where `.window__content img` was overriding specific image sizes, which caused both the Calendar stickers and the "Prima di Photoshop" image to break.
- Restored Calendar sticker sizes to the original design (140px) and fixed the "Prima di Photoshop" preview window to perfectly match its intrinsic aspect ratio (420x588) so it fits edge-to-edge without zooming or cropping.

Session Summary (2026-09-22)
- Updated Crispy TVC videos, removing the old ones and linking the 4 new ones.
- Fixed folder label alignment in FinderWindow to properly center multiline text.
- Adjusted Calendar and StickyNote initial positions to correctly appear in the lower part of the screen, with the sticky note offset to the right.

Session Summary (2026-09-23)
- Renamed TVC and PED media files (7DAYS, ILIAD, Oro Saiwa, and 10/10_MA) both in the filesystem and within the application's virtual file system components (QuickTimePlayer, ImageViewer, FinderWindow).
- Reordered media items within the ILIAD and PED collections as requested.
- Updated dock tooltips (quips) for Finder, Chrome, Mail, Photoshop, Illustrator, and Outlook.
- Fixed a bug where double-clicking a desktop folder while its Finder window was hidden (minimized) but its InfoPanel was open would not un-minimize the Finder window.
- Fixed a UI artifact/seam in FinderWindow by moving the backdrop-filter blur from the individual sidebar components up to the main window wrapper.
- Uniformed border-radius to 16px across all windows and panels (`Window`, `FinderWindow`, `ImageViewer`, `QuickTimePlayer`, `InfoPanel`, `CalendarApp`).
- Refactored Finder toolbar to place the folder title on a single line adjacent to navigation arrows with 10px gap and ellipsis (`...`) text truncation.
- Fixed Finder layout step (gradino) issue by ensuring the sidebar remains fixed-width with `flex-shrink: 0`.
- Uniformed the default "Prima di Photoshop usavo i pastelli.png" window with other image viewers using `ImageViewer`.
- Disabled hover sidebar in `ImageViewer` when viewing a single image collection.
- Sized the "Prima di Photoshop" window to 810x567px (reduced by 10% from 900x630px).
